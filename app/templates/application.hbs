<header class="app--header">
  <h1>Perma Projects</h1>
  <h2>Permaculture Projects from around the world</h2>
  <nav>
    {{#if session.isAuthenticated}}
      Logged in as {{session.currentUser.displayName}}
      {{#link-to "new"}}Add your Project{{/link-to}}
      <button {{action "signOut"}}>Sign Out</button>
    {{else}}
      <button {{action "signIn" "facebook"}}>Sign in with Facebook</button>
    {{/if}}
  </nav>
</header>
<main>
{{outlet}}
</main>
<footer></footer>
