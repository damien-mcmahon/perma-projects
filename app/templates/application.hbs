<header class="app--header">
  <h1 class="app--header--name">
    {{#link-to "index"}}Perma Projects{{/link-to}}
  </h1>
  <h2 class="app--header--subtitle">Permaculture Projects from around the world</h2>
  <nav class="app--header--nav">
    {{#link-to "about"}}About{{/link-to}}
    {{#if session.isAuthenticated}}
      {{#link-to "new"}}New Projects{{/link-to}}
      Logged in as {{session.currentUser.displayName}}
      <button {{action "signOut"}}>Sign Out</button>
    {{else}}
      <button {{action "signIn" "facebook"}}>Sign in with Facebook</button>
    {{/if}}
  </nav>
</header>
<main class="app--main">
  {{outlet}}
</main>
<footer></footer>
